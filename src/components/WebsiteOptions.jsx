import { useState } from 'react';
import { motion } from 'framer-motion';
import { ArrowUpRight } from 'lucide-react';
import { websiteOptions } from '../data/siteContent';
import SectionHeader from './SectionHeader';
import WebsiteOptionModal from './WebsiteOptionModal';
export default function WebsiteOptions(){const [selected,setSelected]=useState(null);return <section id="options" className="px-5 py-24"><div className="mx-auto max-w-7xl"><SectionHeader eyebrow="Website options" title="Choose the kind of website your business needs." text="Explore polished website concepts that can be shaped around your industry, content and growth goals."/><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{websiteOptions.map((o,i)=><motion.article initial={{opacity:0,y:24}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*.03}} whileHover={{y:-8}} className="luxury-card rounded-[1.75rem] p-6" key={o.title}><div className="mb-5 h-36 rounded-[1.4rem] bg-gradient-to-br from-white/10 via-[#d8b56d]/10 to-transparent p-4"><div className="h-full rounded-2xl border border-white/10 bg-black/20"/></div><h3 className="text-2xl font-semibold text-[#fff6dd]">{o.title}</h3><p className="mt-3 text-[#c9c0ad]">{o.description}</p><p className="mt-4 text-sm text-[#d8b56d]">Best for: {o.bestFor}</p><ul className="mt-4 flex flex-wrap gap-2">{o.features.map(f=><li className="rounded-full bg-white/5 px-3 py-1 text-xs text-[#ddd2bc]" key={f}>{f}</li>)}</ul><button onClick={()=>setSelected(o)} className="mt-6 inline-flex items-center gap-2 rounded-full border border-white/15 px-5 py-3 font-semibold text-[#fff6dd] transition hover:border-[#d8b56d]/60">View details <ArrowUpRight size={16}/></button></motion.article>)}</div></div><WebsiteOptionModal option={selected} onClose={()=>setSelected(null)}/></section>}
