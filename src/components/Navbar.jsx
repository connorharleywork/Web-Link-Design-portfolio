import { Menu, X } from 'lucide-react';
import { useEffect, useState } from 'react';
import { brand, navLinks } from '../data/siteContent';
const go = (id) => document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' });
export default function Navbar(){ const [open,setOpen]=useState(false); const [scrolled,setScrolled]=useState(false); useEffect(()=>{const on=()=>setScrolled(scrollY>12); on(); addEventListener('scroll',on); return()=>removeEventListener('scroll',on)},[]);
return <header className={`fixed inset-x-0 top-0 z-50 transition ${scrolled?'glass':''}`}><nav className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8"><button onClick={()=>go('home')} className="text-left"><span className="block text-lg font-semibold text-[#fff6dd]">{brand.name}</span><span className="hidden text-xs text-[#d8b56d] sm:block">Premium web design studio</span></button><div className="hidden items-center gap-6 lg:flex">{navLinks.map(([l,id])=><button className="text-sm text-[#d8d0bf] transition hover:text-[#fff6dd]" key={id} onClick={()=>go(id)}>{l}</button>)}<button onClick={()=>go('contact')} className="rounded-full bg-[#d8b56d] px-5 py-2.5 text-sm font-semibold text-[#17130b] shadow-[0_0_30px_rgba(216,181,109,.25)] transition hover:scale-105">Start Your Website</button></div><button aria-label="Open menu" onClick={()=>setOpen(!open)} className="rounded-full border border-white/10 p-2 lg:hidden">{open?<X/>:<Menu/>}</button></nav>{open&&<div className="glass mx-4 mb-4 rounded-3xl p-4 lg:hidden">{navLinks.map(([l,id])=><button className="block w-full rounded-2xl px-4 py-3 text-left text-[#eee3ca] hover:bg-white/5" key={id} onClick={()=>{go(id);setOpen(false)}}>{l}</button>)}</div>}</header>}
